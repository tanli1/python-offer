题目
输入两个链表，找出它们的第一个公共结点。

思路
公共结点的样子：

A:     a1 -> a2 
                \
                 c1  -> c2 -> c3
                /
B: b1 -> b2 -> b3

上图就是一个有公共结点的例子，在公共结点之后，两个链表指针指向的地址是相同的。


两个链表一一做对比，查看是否相同。